import React, {Component} from 'react';

export default class DrinkRecord {
    constructor() {}

    createDrinkRecord(drink, startTime) {
        let drinkRecordId;

        // Create drink record and return id

        return drinkRecordId;
    }

    listDrinkRecords(start, end = Math.floor(Date.now() / 60000)) {
        // Get all records from given time range
    }

    getDrinkRecord(id) {
        let drinkRecord;

        // Get drink from db

        return drinkRecord;
    }
}
